server:
  port: 8080
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: postgres

  jpa:
    properties:
      hibernate:
        hbm2ddl:
          auto: validate

app:
  image:
    bucket: /home/study/IdeaProjects/my-multimodule-project/images

